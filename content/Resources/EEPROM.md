#resource #hardware #software 

EEPROM (==Electrically Erasable Programmable Read-Only Memory==) is non-volatile, byte-addressable memory used to store small amounts of data (e.g., configuration, calibration) that must survive power loss. It uses electrical signals for byte-level erasure and reprogramming, with a limited endurance of typically ~1 million cycles before degradation. 