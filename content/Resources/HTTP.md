#software #networks 

HTTP is a set of rules for how one program asks another program for something over the network, and how the other program replies.

Client → Request → Server
Client ← Response ← Server

HTTP rides on [[TCP]]

Important connection to last class:
**TCP** → reliable pipe
**HTTP** → rules about what flows through the pipe

**HTTP has 4 verbs only**
get
put
post
delete