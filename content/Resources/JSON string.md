#software 

“A shared language for structured data”
JSON is a text format for sending structured data in a way both humans and machines can read. It’s a string of characters that _represents_ structured data. Arduino can generate it, JavaScript can parse it instantly, MQTT loves it and HTTP APIs expect it.