We started with annotating code by Ellen
We talked about the command line - as a way to send files to git for version control.

`nano` -> to edit text.txt files in the terminal.

We tried understanding code in p5 just by reading it and then sketching, I didnt do so well...